import {
  it,
  inject,
  describe,
  beforeEachProviders,
  expect
} from '@angular/core/testing';
import GameComponent from './GameComponent';
describe('App', () => {
  beforeEachProviders(() => [
    GameComponent
  ]);
  it ('should work', inject([GameComponent], (app: GameComponent) => {
    // Add real test here
    expect(2).toBe(2);
  }));
});
